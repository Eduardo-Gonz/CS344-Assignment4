How to compile
1. open terminal and navigate to the correct folder
2. Run this command in terminal to compile: 'gcc -pthread --std=gnu99 -o line_processor main.c'
3. Use this command to run the program: './line_processor'
4. Enjoy using the program :)